{
  "name": "market-sentinel",
  "private": true,
  "description": "Repo de complementos HAOS + desarrollo. El add-on vive en centinela/.",
  "scripts": {
    "dev": "npm run dev --prefix centinela",
    "dev:worker": "npm run dev:worker --prefix centinela",
    "dev:ui": "npm run dev:ui --prefix centinela",
    "start": "npm run start --prefix centinela",
    "start:preview": "npm run start:preview --prefix centinela",
    "build:ui": "npm run build:ui --prefix centinela",
    "typecheck": "npm run typecheck --prefix centinela"
  }
}
